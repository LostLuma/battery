[package]
name = "battery-jni"
version = "1.0.0"
edition = "2021"

[dependencies]
jni = "0.21.1"
starship-battery = "0.10.0"

[lib]
crate-type = ["cdylib"]
path = "src/main/rust/lib.rs"

[profile.release]
lto = true
strip = true
codegen-units = 1
